[package]
name = "llvm-sys-test"
version = "0.0.0"
edition = "2021"
publish = false

[build-dependencies]
llvm-full = { path = "../..", features = ["llvm18-1"] }

[dependencies]
llvm-sys = { version = "181", features = ["no-llvm-linking"] }
